# Trigger for the centralised review sync (Lascade-Co/store-reviews).
# Copy this file into the app repo at .github/workflows/review-sync-trigger.yml
# and set "project_slug" to the app's Infisical project slug.
# Full setup guide: review.md in the central repo.

name: Trigger Review Sync

on:
  workflow_dispatch:
  schedule:
    # 06:00 IST daily (cron is UTC, IST = UTC+5:30). Same cron for every
    # app — do not edit; the stagger step below spreads apps out automatically.
    - cron: "30 0 * * *"

permissions:
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      # Repo-specific deterministic 0-5 min delay so apps sharing the cron do
      # not all hit the central workflow at the same instant. Skipped on manual
      # runs. Logs the delay and a per-minute countdown.
      - name: Stagger start (repo-specific delay; skipped on manual runs)
        if: github.event_name == 'schedule'
        run: |
          OFFSET=$(( $(cksum <<< "$GITHUB_REPOSITORY" | cut -d' ' -f1) % 300 ))
          DISPATCH_AT=$(date -u -d "+${OFFSET} seconds" '+%H:%M:%S')
          echo "Repository:       ${GITHUB_REPOSITORY}"
          echo "Stagger delay:    ${OFFSET}s (deterministic, derived from the repo name)"
          echo "Current time:     $(date -u '+%Y-%m-%d %H:%M:%S') UTC"
          echo "Dispatching at:   ${DISPATCH_AT} UTC"
          REMAINING=${OFFSET}
          while [ "${REMAINING}" -gt 0 ]; do
            STEP=$(( REMAINING < 60 ? REMAINING : 60 ))
            echo "$(date -u '+%H:%M:%S') UTC | waiting, ${REMAINING}s left (dispatch at ${DISPATCH_AT} UTC)"
            sleep "${STEP}"
            REMAINING=$(( REMAINING - STEP ))
          done
          echo "$(date -u '+%H:%M:%S') UTC | stagger complete, dispatching now"

      - name: Dispatch to central review-bot
        uses: peter-evans/repository-dispatch@v4
        with:
          # PAT/GitHub-App token allowed to dispatch to Lascade-Co/store-reviews.
          token: ${{ secrets.STORE_REVIEWS_DISPATCH_TOKEN }}
          repository: Lascade-Co/store-reviews
          event-type: review-sync
          # project_slug is the app's Infisical project slug
          client-payload: >-
            { "project_slug": "the infisical project slug" }
